groups:
  - name: master
    jobs:
      - development
      - qa
      - int
  - name: pull-request
    jobs:
      - dataworks-rbac-munge-policies-infra-pr
  - name: update-pipeline
    jobs:
      - update-pipeline
